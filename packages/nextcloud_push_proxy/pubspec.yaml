name: nextcloud_push_proxy
version: 1.0.0

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  shelf: ^1.3.1
  shelf_router: ^1.1.3

dev_dependencies:
  crypto: ^3.0.2
  nit_picking:
    git:
      url: https://github.com/stack11/dart_nit_picking
      ref: f29382f
